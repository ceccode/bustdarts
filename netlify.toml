[build]
  command   = "npm run build"
  publish   = "dist"

[build.environment]
  NODE_VERSION = "20"

# SPA fallback: every non-asset request serves index.html
[[redirects]]
  from   = "/*"
  to     = "/index.html"
  status = 200
